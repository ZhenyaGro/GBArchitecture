{
  "name": "gbarchitecture",
  "version": "0.0.1",
  "description": "GB architecture course but using TypeScript",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc ./src/index.ts --outdir dist --target es2015",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Architecture"
  ],
  "author": "ZhenyaGro",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
